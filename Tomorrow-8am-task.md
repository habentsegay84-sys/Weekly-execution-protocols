# 4 AM TASK: Write Worst AI Code

## Requirements:
1. 5+ intentional errors
2. Runs without crash (even if wrong)
3. Terrible variable names
4. Commit regardless of quality

## Python Example (Start with this):
```python
# WORST NEURAL NETWORK - DAY 1
import wrong_library  # doesn't exist

def neural_network(x):
    # Always return wrong answer
    return "not a number"  # should return number
    
def train(data):
    # Make model worse
    weights = [0, 0, 0]  # never learns
    return -1  # negative progress

# Intentional errors:
# 1. Wrong import
# 2. Wrong return type
# 3. Weights never update
# 4. Negative progress
# 5. No actual learning
